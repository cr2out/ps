쌍마다 서로소 = 각 소수의 소유권을 최대 한 명에게 배분하는 문제
"lcm = 곱" ⇔ 쌍마다 서로소 ⇔ 각 소수는 한 사람만 소유 가능 → 소수별로 (1 + 지수합)

배열 원소 단위로 보는관점을 소수 관점으로 보자
-------------------------------------------------------
인접한 2개 원소에 관한 어떠한 연산을 통해서 최소/최대값을 구하고 원소 삭제를 한다면 std::set 이용.
std::set 자동 오름차순 됨. insert, erase 하면 됨.
https://codeforces.com/contest/2166/problem/C 
말하는건데 굳이 set or linked_list 안써도 됐긴함.
-------------------------------------------------------
빠진 원소의 비트를 자리별 카운팅으로 확정, 상위 비트로 갈수록 하위 비트 일치 후보만 재쿼리 → 예산 n+n/2+... = 2n
이라고 클로드가 적으라고 했음.

쿼리 개수 제한이 2n 개 이면 등비수열(n + n/2 + n/4 + n/8 + ...) 형태 생각해보자
--------------------------------------------------------
문제를 읽고 연속인지 아닌지부터 확정 — "subarray/substring/구간/연속" vs "subsequence/부분수열". 이걸 잘못 읽으면 접근 전체가 어긋나서, 그 자체로 흔한 늪이에요.
연속이면 → 고정 축 4가지 훑기
비연속이면 → DP/그리디 계열 훑기

연속이면
길이 k를 고정하고 윈도우를 민다
왼쪽 끝 i를 고정하고 오른쪽을 확장한다
오른쪽 끝 j를 고정하고 유효한 왼쪽 범위를 관리한다 (투 포인터)
값 기준으로 고정한다 (예: min이 되는 원소를 고정)

비연속이면 DP/그리디
-------------------------------------------------------
https://codeforces.com/contest/2242/problem/C
모든 대상에 똑같은 연산(전부 +1 / 전부 -1)이 가해지는 구조면
상태를 낱낱이 추적하지 말고 "원래 값들 + 공통 오프셋 e" 하나로 매개변수화하자.
런 크기들 (c1,...,cs) → (c1+e, ..., cs+e), 전체 길이 = suffix + s·e
도달 가능 길이 = 등차수열 { suffix + s·e : e ≥ -(w-1) }, w = 최소 런 크기
→ 판정은 딱 두 개: k ≡ suffix (mod s), k ≥ suffix - s(w-1) -> 하한

mod s 세계에서 s의 배수 항(s·d)은 어차피 사라진다.
"진입 시점 실제 길이"를 앵커로 잡고 깊이 d를 추적하려는 시뮬레이션 직관이 함정.
앵커는 그냥 원래 크기 합(suffix)으로 고정하면 됨 → 뺄 때도 w * 중복개수만 빼면 끝.

중복 카운트 의심되면 flag 같은 보정 트릭 넣기 전에 "애초에 겹칠 수 있는가"부터 증명.
여기선 생존 집합마다 값 구성이 달라서 절대 안 겹침 → 그냥 독립적으로 +1.
버그 두 개가 서로 상쇄해서 샘플 통과하면 "엣지케이스"가 아니라 "구조가 틀렸다"를 의심.

요약 : 하한을 생각해보자.
       모든 집합들의 원소를 똑같이 추가하거나 삭제하는 연산이 있을때, 집합 자체를 없앨 일이 생기면 그냥 삭제할려는 집합만 삭제해도 됨.
       왜냐면 원래 연산대로 전체 집합에서 원소를 똑같이 삭제시키면 그 집합이 사라지고, 그 후에 남은 집합끼리 원소 추가하는 연산하면 그냥 삭제할려는 집합만 삭제한거랑 모양 똑같이 만들 수 있음.
       flag 같은 보정 트릭 쓰기전에 안쓰는 방법 생각해보자. (case 처리는 코드를 복잡하게 만든다)

-------------------------------------------------------
최대값 prefix 배열 역순으로 만들어야할 일 생기면 정순으로 min을 쓰는 방안도 고려를 해보는게 어떨까.
prefix 배열 쓰기전에 축을 생각해보자(위에 고정관련 글 내용)
-------------------------------------------------------
누적한 값을 담은 배열이 n크기로 있을 때 i~n까지의 누적 최솟값 위치랑 1~n 누적 최솟값 위치랑 같다.
-------------------------------------------------------
subsequence 관련 답 = 원래 길이 − 지운 개수"로 재정의하기
불변량(괄호 균형 = (와 ) 개수 차)으로 지워야 할 최소량 계산하기
-------------------------------------------------------
양의 정수 x가 있을때 [x,2x] 범위에 소수 p가 무조건 존재함.
순열 배열해서 sum(1~i)/i 같은 수 만들려면 x x+1 x-1 x+2 x-2 x+3 x-3 이런식으로 배열하면 됨.
-------------------------------------------------------
항상 최적해의 모양을 생각해보자.
수들을 묶는 문제인 경우, 묶는 방식의 최소 원소를 생각해보자. (최적해 형태 제한)
https://codeforces.com/contest/2153/problem/D
-------------------------------------------------------
노드가 기준이 아니라 간선이 기준일 수 있다.
그래프 문제인데 탐색 순서가 어떤 값을 기준으로 정렬한 것 일 수도 있다.
방향 그래프에서 모든 (2개 이상 정점) 경로의 개수는 dp로 구할 수 있다.
dp[내 노드가 도착점 일 때 도달 가능한 경우의 수] = sigma (j=1 ~ 부모 노드 수), dp[j번째 부모 노드가 도착점일 때 도달 가능한 경우의 수] + 1
그 후 모든 노드의 dp 값을 다 더하면 됨.
https://codeforces.com/contest/2176/problem/D
-------------------------------------------------------
구성 조건을 흩어서 체크하지 말고, 마지막에 정의를 통째로 재검증할 수 있으면 그렇게 해라.
prefix max / prefix sum 복원 문제는 원래 조건을 b에 대한 등식+부등식 집합으로 동치 변환하고, 고정된 차분이 변수들을 체인으로 묶어 자유도를 세는 것부터 시작한다.
https://codeforces.com/contest/2231/problem/D
-------------------------------------------------------
체크되는 인덱스는 별로 없는데 인덱스의 범위가 넓으면 map을 사용해보자
https://codeforces.com/contest/2154/problem/C1
-------------------------------------------------------
큰수a / 큰수b 해야할 때 모듈러 역원사용
b가 어떤 수들의 곱이라면 그냥 곱할 때 % MOD 하고 역원 구한뒤 a * b역원 % MOD 하면 됨.
-------------------------------------------------------
간선 당 어떤 값을 선택해야하는 문제가 있다면 간선의 방향을 선택안하는쪽->선택하는쪽(반대방향도 가능, 단 모든 간선에 대하여 일관성이 있어야함)으로 그래프 만들고 위상정렬 고려해보자.
-------------------------------------------------------
구성형 문제는 "최대에서 깎기"보다 "최소에서 쌓기"를 먼저 시도한다. 쌓기는 각 원소의 기여가 독립적이라 부분합 문제로 환원되지만, 깎기는 이미 만들어진 구조에서 원소를 빼낸 뒤 재연결해야 하고 그 재연결 지점이 예외의 온상이다.
https://codeforces.com/contest/2120/problem/C
-------------------------------------------------------
구간 연산을 해도 접두, 접미사 배열 의심해보고 연산이 양쪽만 건드려도 접두, 접미사 배열 의심 해보자.
https://codeforces.com/contest/2064/problem/C
https://codeforces.com/contest/2175/problem/B
-------------------------------------------------------
MEX랑 원소 >= 1 이고 원소 순서대로 합하는 문제있으면 원소는 무조건 1.
https://codeforces.com/contest/2059/problem/C
-------------------------------------------------------
벡터 매개변수로 놔두면 함수 스택 쌓일때마다 벡터 복사돼서 &(참조)붙여야 함.
그래프 인접리스트 같은거 쓸 때 그냥 전역으로 쓰자.
-------------------------------------------------------
구간으로 비트 뒤집는 문제 바운더리(값이 바뀌는 지점) 를 생각해보자. 그리고 바운더리를 없애는 방향으로 하자.
짝지어서 없애는 문제 있을때는 최댓값 > 전체 - 최댓값 라면 무조건 남는다.
예 ) 2 | 2 | 5 -> 5 > 4
https://codeforces.com/contest/2217/problem/D
-------------------------------------------------------
[전체합 이중계산 (double counting) — 제약 1개 절약]
마지막 행/열 제약이 자동 충족되는 메커니즘:
  T를 행 기준, 열 기준으로 두 번 세면
  (앞의 n-1개 행 합) + r_n = (앞의 m-1개 열 합) + c_m
  앞부분이 모두 0이면 r_n = c_m 이 무조건 성립.
https://codeforces.com/contest/2055/problem/C
-------------------------------------------------------
수열을 연속되게 m + 1개의 부분수열로 묶고 한개의 부분수열말고 다 서로 인접해있을 때 왼쪽에서 k개 묶으면 오른쪽에서 m - k개 묶으면 됨.
https://codeforces.com/contest/2028/problem/C
-------------------------------------------------------
겉보기에 O(n^2) DP 같은 애 상태를 좀 다양하게 표현하면 까고보면 O(n) DP 일 수 있다.
https://codeforces.com/contest/2022/problem/B
-------------------------------------------------------
최대값/최소값 문제 하한/상한을 찾고 충분성 검증하면 쉽게 풀릴 수 있음.
https://codeforces.com/contest/2022/problem/B
-------------------------------------------------------
경우의 수 state(abc 가 순서대로 포함된 문자열 등등...) 를 이용한 dp 로 구할 때 state를 각 원소가 처음 나온 시점 기준으로 한다고 생각하면 편함.
https://codeforces.com/contest/2191/problem/D2
-------------------------------------------------------
비둘기집 원리 이용할 때, a개의 위치에 모두 중복되는 숫자가 없기까지의 경우의 수는 나머지 위치에 원소 들어가는 순서들은 상관이 없다.
컴비네이션 계산할 때 이미 i가 너무 크면 %MOD 하고 곱하자.
https://codeforces.com/contest/2120/problem/D
